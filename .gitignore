# Python
__pycache__/
.venv/
venv/

# a8s mailboxes and filedrop seats
**/.files/
**/.inbox/
**/.outbox/
**/.trash/

# Harness local settings
**/.claude/settings.local.json

# PII patterns (local only — CI uses the GitHub secret PII_PATTERNS)
.github/pii-patterns.local.txt

# macOS
.DS_Store
.claude/

# The resident seat's own files: identity, memory, and the engine-run
# working-state pair. Never tracked, so a headless turn can live in the checkout.
.ares/
/STATUS.md
/LESSONS.md
/LESSONS-ARCHIVE.md
