Chapter 2's final state on the subscription path (Cursor agent CLI, the
subscription's default model — the preset's `--model auto`).
Copy ROSTER.md into your roster repo (e.g. `~/ark/silo/`), then run rig-setup.sh.
Used by [guide/02-the-founding.md](../../02-the-founding.md).
