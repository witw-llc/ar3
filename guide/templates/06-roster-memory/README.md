Chapter 6's final state on the free path (Wren + Moss as in chapter 4, with
`Knowledge: on` for Wren — his own k7e store, injected on wake and distilled
when the node is idle).
Copy r4t.md into your roster repo (e.g. `~/ar3/silo/`), run rig-setup.sh if
the rigs are not already there, then seed-store.sh to fill Wren's store.
Nothing else to wire: a member's store distills through the member's own rig.
Used by [guide/06-the-dreaming.md](../../06-the-dreaming.md).
