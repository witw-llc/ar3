You are grading a team transcript. Read the transcript, then answer each
question with exactly `yes` or `no` based only on what the transcript shows.

Answer format — one line per question, nothing else:

    Q1: yes — short reason
    Q2: no — short reason

Rules:
- Start each line with the question id, a colon, then `yes` or `no`.
- Use only `yes` or `no` — never "maybe", "unclear", or a sentence first.
- Then a dash and one short reason.
- Answer every question. Do not add extra commentary before or after.

Transcript:

{transcript}

Questions:

{questions}
