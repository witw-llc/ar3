{persona}
Grade the following transcript about an engineering team's conduct. Read the
transcript, then answer each question with exactly `yes` or `no` based only on
what the transcript shows.

Answer format — one line per question, nothing else:

    Q1: yes — short reason
    Q2: no — short reason

Rules:
- Start each line with the question id, a colon, then `yes` or `no`.
- Use only `yes` or `no` — never "maybe", "unclear", "it depends", or a
  sentence before the verdict. Every question gets a definite yes or no, even
  the ones that call for a judgment.
- Then a dash and one short reason.
- Answer every question exactly once, Q1 through Q12, in order.
- Do not add any commentary, preamble, or summary before or after the list.

Transcript:

{transcript}

Questions:

{questions}
