{
  "description": "Codex via `ollama launch`, as a bare stateless engine node \u2014 the same three wakes as engine-ollama-codex, invoked with --permissions bypass, which for codex is --dangerously-bypass-approvals-and-sandbox: the approvals and the sandbox both go, so the turn is no longer confined to its workspace. Requires a8s var MODEL (`a8s add ... --model=...` or `a8s vars <name> set MODEL ...`). An unrestricted node acts on untrusted inbound mail \u2014 anyone who can reach its inbox, including over a broker or a shared folder, is driving a CLI that will not ask. Only for an agent on its own machine and its own account. Turn timeout defaults to 900s; override per node with `a8s vars <name> set TIMEOUT <secs>`. Set EFFORT to an engine-supported reasoning level; unset uses the engine default.",
  "invoke": [
    "$PYTHON",
    "$A8S_DIR/../r4t/r4t.py",
    "engine",
    "ollama-codex",
    "run",
    "--timeout=$TIMEOUT?",
    "--effort=$EFFORT?",
    "--permissions",
    "bypass",
    "--agent",
    "$RECIPIENT",
    "--model",
    "$MODEL",
    "[$NOW] $SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ],
  "batch": {
    "limit": 20,
    "invoke": [
      "$PYTHON",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "ollama-codex",
      "run",
      "--timeout=$TIMEOUT?",
      "--effort=$EFFORT?",
      "--permissions",
      "bypass",
      "--agent",
      "$RECIPIENT",
      "--model",
      "$MODEL"
    ]
  },
  "idle": {
    "timeout": 900,
    "invoke": [
      "$PYTHON",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "ollama-codex",
      "run",
      "--timeout=$TIMEOUT?",
      "--effort=$EFFORT?",
      "--permissions",
      "bypass",
      "--idle",
      "--agent",
      "$RECIPIENT",
      "--model",
      "$MODEL"
    ]
  }
}
