{
  "description": "Codex CLI default — --sandbox workspace-write, fresh session every wake.",
  "invoke": [
    "codex", "exec",
    "--sandbox", "workspace-write",
    "--skip-git-repo-check",
    "[$NOW] $SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ]
}
