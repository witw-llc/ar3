{
  "description": "Claude Code via `ollama launch`, as a bare stateless engine node \u2014 `r4t engine ollama-claude run`, one headless turn per wake with no roster or dispatcher, local model, no cloud quota; STATUS.md/LESSONS.md in the node's own root are its only memory. Requires a8s var MODEL (`a8s add ... --model=...` or `a8s vars <name> set MODEL ...`); tune it further with --permissions and --allowed-tools on the invoke lines below; `r4t engine ollama-claude check` proves the argv still parses.",
  "invoke": [
    "python3",
    "$A8S_DIR/../r4t/r4t.py",
    "engine",
    "ollama-claude",
    "run",
    "--agent",
    "$RECIPIENT",
    "--model",
    "$MODEL",
    "$SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ],
  "batch": {
    "limit": 20,
    "invoke": [
      "python3",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "ollama-claude",
      "run",
      "--agent",
      "$RECIPIENT",
      "--model",
      "$MODEL"
    ]
  },
  "idle": {
    "timeout": 900,
    "invoke": [
      "python3",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "ollama-claude",
      "run",
      "--idle",
      "--agent",
      "$RECIPIENT",
      "--model",
      "$MODEL"
    ]
  }
}
