{
  "description": "OpenCode via `ollama launch`, as a bare stateless engine node \u2014 `r4t engine ollama-opencode run`, one headless turn per wake with no roster or dispatcher, local model, no cloud quota; STATUS.md/LESSONS.md hold working state; enable private K7E memory with `a8s vars <name> set MEMORY on`. Requires a8s var MODEL (`a8s add ... --model=...` or `a8s vars <name> set MODEL ...`); tune it further with --permissions on the invoke lines below; `r4t engine ollama-opencode check` proves the argv still parses. Turn timeout defaults to 900s; override per node with `a8s vars <name> set TIMEOUT <secs>`. Set EFFORT to an engine-supported reasoning level; unset uses the engine default.",
  "invoke": [
    "$PYTHON",
    "$A8S_DIR/../r4t/r4t.py",
    "engine",
    "ollama-opencode",
    "run",
    "--memory=$MEMORY?",
    "--memory-home=$MEMORY_HOME?",
    "--memory-writer=$MEMORY_WRITER?",
    "--memory-people=$MEMORY_PEOPLE?",
    "--timeout=$TIMEOUT?",
    "--effort=$EFFORT?",
    "--agent",
    "$RECIPIENT",
    "--model",
    "$MODEL",
    "[$NOW] $SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ],
  "batch": {
    "limit": 20,
    "invoke": [
      "$PYTHON",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "ollama-opencode",
      "run",
      "--memory=$MEMORY?",
      "--memory-home=$MEMORY_HOME?",
      "--memory-writer=$MEMORY_WRITER?",
      "--memory-people=$MEMORY_PEOPLE?",
      "--timeout=$TIMEOUT?",
      "--effort=$EFFORT?",
      "--agent",
      "$RECIPIENT",
      "--model",
      "$MODEL"
    ]
  },
  "idle": {
    "timeout": 900,
    "invoke": [
      "$PYTHON",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "ollama-opencode",
      "run",
      "--memory=$MEMORY?",
      "--memory-home=$MEMORY_HOME?",
      "--memory-writer=$MEMORY_WRITER?",
      "--memory-people=$MEMORY_PEOPLE?",
      "--timeout=$TIMEOUT?",
      "--effort=$EFFORT?",
      "--idle",
      "--agent",
      "$RECIPIENT",
      "--model",
      "$MODEL"
    ]
  }
}
