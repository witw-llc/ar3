{
  "description": "Cursor Agent CLI as a bare stateless engine node — `r4t engine cursor run`, one headless turn per wake with no roster or dispatcher; STATUS.md/LESSONS.md in the node's own root are its only memory. Tune it with --permissions on the invoke lines below; `r4t engine cursor check` proves the argv still parses.",
  "invoke": [
    "python3",
    "$A8S_DIR/../r4t/r4t.py",
    "engine",
    "cursor",
    "run",
    "--agent",
    "$RECIPIENT",
    "$SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ],
  "batch": {
    "limit": 20,
    "invoke": [
      "python3",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "cursor",
      "run",
      "--agent",
      "$RECIPIENT"
    ]
  },
  "idle": {
    "timeout": 900,
    "invoke": [
      "python3",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "cursor",
      "run",
      "--idle",
      "--agent",
      "$RECIPIENT"
    ]
  }
}
