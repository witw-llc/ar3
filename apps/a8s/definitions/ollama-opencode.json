{
  "description": "OpenCode via `ollama launch` — local models, no cloud quota. Fresh session every wake; --auto for headless tool use. Requires a8s var MODEL (`a8s add ... --model=...` or `a8s vars <name> set MODEL ...`).",
  "invoke": [
    "ollama",
    "launch",
    "opencode",
    "--model",
    "$MODEL",
    "--",
    "run",
    "--auto",
    "$SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ]
}
