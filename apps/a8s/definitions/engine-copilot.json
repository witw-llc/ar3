{
  "description": "GitHub Copilot CLI as a bare stateless engine node \u2014 `r4t engine copilot run`, one headless turn per wake with no roster or dispatcher; STATUS.md/LESSONS.md in the node's own root are its only memory. Tune it with --permissions on the invoke lines below (no --continue: copilot's session is machine-global, not per-agent \u2014 #17); `r4t engine copilot check` proves the argv still parses.",
  "invoke": [
    "$PYTHON",
    "$A8S_DIR/../r4t/r4t.py",
    "engine",
    "copilot",
    "run",
    "--agent",
    "$RECIPIENT",
    "[$NOW] $SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ],
  "batch": {
    "limit": 20,
    "invoke": [
      "$PYTHON",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "copilot",
      "run",
      "--agent",
      "$RECIPIENT"
    ]
  },
  "idle": {
    "timeout": 900,
    "invoke": [
      "$PYTHON",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "copilot",
      "run",
      "--idle",
      "--agent",
      "$RECIPIENT"
    ]
  }
}
