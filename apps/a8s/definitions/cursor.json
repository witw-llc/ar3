{
  "description": "Cursor Agent CLI default — -p print mode for headless wakes, --trust + --force for unattended tool use. Fresh session every wake.",
  "invoke": [
    "agent",
    "-p",
    "--trust",
    "--force",
    "--approve-mcps",
    "[$NOW] $SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ]
}
