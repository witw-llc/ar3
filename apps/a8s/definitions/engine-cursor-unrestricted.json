{
  "description": "Cursor Agent CLI as a bare stateless engine node \u2014 the same three wakes as engine-cursor, invoked with --permissions bypass. Cursor's strongest mode is auto and the base preset already composes it, so this definition is identical to engine-cursor today; the name states the intent, and it picks up any stronger mode cursor grows. Pick a model with `a8s vars <name> set MODEL <id>` (or `a8s add ... --model=<id>`); unset means the engine's own default. An unrestricted node acts on untrusted inbound mail \u2014 anyone who can reach its inbox, including over a broker or a shared folder, is driving a CLI that will not ask. Only for an agent on its own machine and its own account. Turn timeout defaults to 900s; override per node with `a8s vars <name> set TIMEOUT <secs>`. Set MODEL with bracket options such as [effort=high] to select reasoning effort. A separate EFFORT variable is unsupported and errors.",
  "invoke": [
    "$PYTHON",
    "$A8S_DIR/../r4t/r4t.py",
    "engine",
    "cursor",
    "run",
    "--memory=$MEMORY?",
    "--memory-home=$MEMORY_HOME?",
    "--memory-writer=$MEMORY_WRITER?",
    "--memory-people=$MEMORY_PEOPLE?",
    "--timeout=$TIMEOUT?",
    "--effort=$EFFORT?",
    "--model=$MODEL?",
    "--permissions",
    "bypass",
    "--agent",
    "$RECIPIENT",
    "[$NOW] $SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ],
  "batch": {
    "limit": 20,
    "invoke": [
      "$PYTHON",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "cursor",
      "run",
      "--memory=$MEMORY?",
      "--memory-home=$MEMORY_HOME?",
      "--memory-writer=$MEMORY_WRITER?",
      "--memory-people=$MEMORY_PEOPLE?",
      "--timeout=$TIMEOUT?",
      "--effort=$EFFORT?",
      "--model=$MODEL?",
      "--permissions",
      "bypass",
      "--agent",
      "$RECIPIENT"
    ]
  },
  "idle": {
    "timeout": 900,
    "invoke": [
      "$PYTHON",
      "$A8S_DIR/../r4t/r4t.py",
      "engine",
      "cursor",
      "run",
      "--memory=$MEMORY?",
      "--memory-home=$MEMORY_HOME?",
      "--memory-writer=$MEMORY_WRITER?",
      "--memory-people=$MEMORY_PEOPLE?",
      "--timeout=$TIMEOUT?",
      "--effort=$EFFORT?",
      "--model=$MODEL?",
      "--permissions",
      "bypass",
      "--idle",
      "--agent",
      "$RECIPIENT"
    ]
  }
}
