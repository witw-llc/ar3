{
  "description": "OpenCode default — BYO model via per-agent opencode.json. Fresh session every wake; --auto for headless tool use.",
  "invoke": [
    "opencode",
    "run",
    "--auto",
    "$SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ]
}
