{
  "description": "Antigravity (agy) default — skip-permissions for headless operation. Fresh session every wake.",
  "invoke": [
    "agy",
    "--dangerously-skip-permissions",
    "-p", "$SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ]
}
