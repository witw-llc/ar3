{
  "description": "GitHub Copilot CLI default — --allow-all-tools (required for non-interactive --prompt mode). No --continue: it is machine-global, not per-agent (#17).",
  "invoke": [
    "copilot",
    "--allow-all-tools",
    "-p", "$SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ]
}
