{
  "description": "GitHub Copilot CLI default — --allow-all-tools (required for non-interactive --prompt mode), resumes most recent session.",
  "invoke": [
    "copilot",
    "--allow-all-tools",
    "--continue",
    "-p", "$SENDER tells $RECIPIENT ($AGE): $MESSAGE"
  ]
}
