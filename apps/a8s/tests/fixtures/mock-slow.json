{
  "description": "Test-only slow mock CLI — sleeps before echoing argv (see mock_slow_cli.py).",
  "invoke": ["$PYTHON", "$A8S_DIR/tests/fixtures/mock_slow_cli.py", "FROM:$SENDER|TO:$RECIPIENT|MSG:$MESSAGE"]
}
